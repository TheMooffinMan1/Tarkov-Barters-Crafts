{
  "name": "tarkov-barters-crafts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive Tarkov craft, barter, and trader-flip profitability tracker",
  "scripts": {
    "test": "node --test compute/test",
    "blob": "node compute/cli.mjs",
    "blob:all": "node compute/cli.mjs --all",
    "dev": "npm run blob -- --mode regular && npm --prefix frontend run dev",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=20"
  }
}
